# Шлюз SiYuan — многопользовательский режим

Шлюз превращает SiYuan в многопользовательский сервис: любой человек регистрируется, получает **личное изолированное рабочее пространство** и может делиться материалами по ссылке `/@логин` — с отдельными пользователями (по паролю) или со всеми.

## Как это работает

```
Интернет ──► шлюз :6810 ──┬── /gw/login, /gw/register, /gw/account  (страницы шлюза)
                          ├── сессия пользователя ──► его ядро SiYuan   (полный доступ)
                          └── /@логин ──► сервис публикации этого ядра  (только чтение)
```

- На каждого пользователя шлюз создаёт папку `users/<логин>/` и запускает отдельный процесс ядра SiYuan. Данные пользователей полностью изолированы друг от друга.
- Порты ядер наружу не публикуются — единственный вход это шлюз. Пароли хранятся как bcrypt-хэши в `users/gateway.json`.
- Шаринг использует родной сервис публикации SiYuan: пользователь сам решает, что видно другим, через дерево документов → меню «Ещё» → **Контроль доступа публикации** (публичный / по паролю / скрытый / приватный / запрещён — на блокнот или документ).

## Запуск

```bash
cd gateway
GATEWAY_INVITE_CODE=секрет docker compose up -d --build
```

Откройте `http://сервер:6810` — появится страница входа, ссылка «Зарегистрироваться» создаёт аккаунт и сразу открывает личное пространство.

Для доступа из интернета поставьте перед шлюзом HTTPS-прокси (nginx/caddy) на порт 6810 (не забудьте проксировать WebSocket `/ws`) и задайте `GATEWAY_SECURE_COOKIE=true`.

## Переменные окружения

| Переменная | По умолчанию | Назначение |
|---|---|---|
| `GATEWAY_LISTEN` | `:6810` | Адрес и порт шлюза |
| `GATEWAY_DATA` | `/siyuan/users` | Корень пользовательских данных |
| `GATEWAY_KERNEL_BIN` | `/opt/siyuan/kernel` | Путь к бинарнику ядра |
| `GATEWAY_LANG` | `ru` | Язык интерфейса новых пространств |
| `GATEWAY_INVITE_CODE` | *(пусто)* | Если задан — регистрация только по коду приглашения |
| `GATEWAY_SECURE_COOKIE` | `false` | `true` при работе за HTTPS-прокси |

## Служебные адреса

| Адрес | Назначение |
|---|---|
| `/gw/login`, `/gw/register` | Вход и регистрация |
| `/gw/account` | Страница аккаунта: своя публичная ссылка, выход |
| `/@логин` | Опубликованные материалы пользователя (только чтение) |
| `/gw/exit-share` | Вернуться из чужой публикации в своё пространство |
| `/gw/logout` | Выйти |

## Ограничения

- Каждое активное ядро занимает ~150–300 МБ ОЗУ; рассчитано на команду до ~20 человек.
- Просмотр чужой публикации «занимает» вкладку браузера целиком (маршрутизация по cookie): чтобы вернуться к своим заметкам, откройте `/gw/exit-share` или страницу аккаунта.
- Настольное/мобильное приложение SiYuan к шлюзу не подключается — как и обычный Docker-вариант, это работа через браузер. Синхронизацию с настольным клиентом каждый пользователь может настроить сам средствами SiYuan (S3/WebDAV).
